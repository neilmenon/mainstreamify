/* 
    Configuration file. Rename to config.ts and fill in fields below
*/

export const config = {
    "project_root": "", // root of project, e.g. http://localhost:4200, https://example.com/myapp
    "api_root": "", // root of the API service e.g. http://localhost:5000/api, https://example.com/api
}